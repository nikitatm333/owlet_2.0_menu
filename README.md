# TMX38_Menu
## Текущая структура работы захвата видеопотока с камеры:
камера пишет кадр → драйвер кладёт его в kernel-буфер → мы mmap его → читаем YUV → конвертим в RGB → пишем в QImage → Qt загружает в GPU → рисует
